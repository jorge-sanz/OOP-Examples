package exercise6;

/**
 * Chronometer is a class which implements a simple chronometer with
 * basic functions like: reset, start, stop.
 * 
 * @author Jorge Sanz
 *
 */
public class Chronometer {
	
	private int  hour, minute, second;
	private String status;
	
	/**
	 * Initializes a Chronometer with hours, minutes and seconds in
	 * zero value (00:00:00) and "reset" status.
	 */
	public Chronometer() {
		this.hour = 0;
		this.minute = 0;
		this.second = 0;
		this.status = "reset";
	}
	
	/**
	 * Get the hour value
	 * 
	 * @return hour Integer value which represents hours
	 */
	
	/**
	 * Set the hour value
	 * 
	 * @param hour Integer value which represents hours
	 */
	
	/**
	 * Get the minute value
	 * 
	 * @return minute Integer value which represents minutes
	 */
	
	/**
	 * Set hours, minutes and seconds to zero value and status to "reset".
	 */
	public void reset() {
		
	}
	
	/**
	 * Make the chronometer  run and change the status to "running".
	 */
	
	/**
	 * Stop the chronometer with the current values and set status to "stopped".
	 */
	
	/**
	 * Print a string with the value of hours, minutes and seconds in the 
	 * current moment.
	 */
}
